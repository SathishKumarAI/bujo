# Session 2026-06-16 — prompt dump & outcomes

Every request from this session, in order, with what shipped. PRs #10–#24 (+ doc #16) all merged to `main` and deployed to <https://bujo-journal.vercel.app> (single canonical alias, re-pointed each deploy).

| # | Prompt (paraphrased) | Outcome |
|---|---|---|
| 1 | Continue your coding | Merged in-flight CSP-enforce → **#10**. |
| 2 | Best way to enter values instead of typing everywhere? | Brainstormed → approved layered design (one local parser, 3 input modes). Spec: `docs/superpowers/specs/2026-06-16-input-capture-design.md`. |
| 3 | Do it | **Phase 1** smart capture bar — `lib/capture.ts` + `CaptureBar` → **#12**. |
| 4 | Merge #11 & #12, start Phase 2 & 3 | Exercise videos **#11**; Phase 2 field controls + Phase 3 voice number parsing → **#13**. |
| 5 | Merge #13, retrofit Trackers & Fitness steppers | Stepper-ized number inputs → **#14**. |
| 6 | Go ahead, next go | Retired QuickAdd + tightened habit matcher → **#15**. |
| 7 | Push asap / is it on Vercel? deploy | Deployed to prod; established single canonical URL. |
| 8 | Check sign-up/login + email options | Verified live via Supabase `/auth/v1/settings`: email/login/guest enabled; **Google disabled**. Live email signup+login smoke test passed. |
| 9 | One prod only, update as you go | Canonical alias `bujo-journal.vercel.app`, re-pointed each deploy. |
| 10 | Draft Google OAuth setup steps | `docs/auth/google-oauth-setup.md` → **#16**. |
| 11 | Mobile overlap / Trackers habit text overflow — fix & document | Sticky habit-name column capped+truncated → **#17**; `docs/qa/mobile-layout-audit.md`. |
| 12 | Good vs bad habits (alcohol etc.) — show differently everywhere | `Habit.avoid` polarity, `cleanStreak`, red/Ban/slip UI in all surfaces → **#18**. |
| 13 | No duplicate data, same-unit, cube tracker broken for check habits, use chrome MCP | Dedup guard + activity/cube cells made interactive (#18); unit-reuse datalist → **#19**. (chrome-devtools MCP offline → used Playwright instead.) |
| 14 | Merge #16; hide long card header text on mobile, reveal on tap | Card subtitles collapse on mobile (ⓘ toggle) → **#23**. |
| 15 | Why does sign-up/in go to a "profile screen"? | Diagnosed: Google OAuth redirect hit a raw Supabase "provider not enabled" JSON page. Hid the Google button until the provider is confirmed enabled → **#23**. |
| 16 | Screenshot all tabs on mobile, check issues | Playwright captured all 16 tabs @390px — zero overflow; minor title-truncation nit noted. |
| 17 | README with screenshots, auto-update after each deploy | README Screenshots section + `npm run shots` + `.github/workflows/screenshots.yml` → **#24**. |
| 18 | Mobile bottom nav: Plan → Pickleball | Swapped in `BottomNav` → **#24**. |
| 19 | Nav regroup: Challenges→Health, Monthly/Collections/Goals/Insights/Stats → "Insights & Stats", combine mobile topbar | Nav restructure + `overflow-x-clip` + trimmed mobile topbar → **#20/#22**. |
| 20 | Layout pass — use space, 2–3 cols, reduce categories | Audited all 19 views; Goals/Challenges/FitnessHub grids + nav de-dup → **#20**. `docs/qa/layout-space-nav-pass.md`. |
| 21 | Deploy, update docs, dump all prompts, update tickets | This document + WORKLOG entry + ticket refresh. |

## Cross-cutting capabilities discovered
- **Playwright works here** (system Chrome `/usr/bin/google-chrome-stable`, routing `?view=<id>`, gate bypass via "This device only") — drive everything in one node script; a persistent Chrome/MCP can't stay up (env reaps detached processes).
- **Mobile overflow root cause** was the TopBar packing too many buttons; fixed with icon-trimming + `overflow-x-clip` on the shell column.

## Still on the user (external switches)
- Enable **Google** provider in Supabase (button auto-reappears) — `docs/auth/google-oauth-setup.md`.
- Delete the smoke-test account `bujo-smoketest-260616@example.com`.
