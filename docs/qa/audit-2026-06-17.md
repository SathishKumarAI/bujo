# Full-codebase audit — 2026-06-17

Four parallel audits (bugs/logic · data/persistence/backups · UI/UX/buttons · docs/prompts). Real findings only; false positives dropped. Fixes applied this pass are ✅; deferred items list the exact fix.

## Fixed ✅

| Area | Bug | Fix |
|---|---|---|
| `store.tsx` toggleHabit | **avoid** habits stamped `habitTimes` → polluted the "When you check in" chart with slips | skip the timestamp write when `habit.avoid` |
| `lib/fasting.ts` | day grouping used `f.end.slice(0,10)` (UTC) while `today` is local → `fastingStreak` off-by-one near midnight in non-UTC zones | new `dayOf()` = local calendar day via `todayISO(new Date())` |
| `lib/achievements.ts` | "7 days clean" keyed on `nofap.best` (only updated on relapse) → a long never-broken streak couldn't earn it | evaluate `max(best, live cleanDays)` |
| `lib/capture.ts` | `[ten]\s+` char-class regex misfired explicit-bullet confidence on words starting t/e/n | `/^(?:[ten]|[*!^])\s+/i` |
| `components/EntryRow.tsx` | important `!` toggle rendered **only when already important** → could un-flag but never flag from the row | render always; dim when off |
| `views/Collections.tsx` | delete `<span onClick>` **nested inside** the toggle `<button>` (invalid HTML, keyboard/SR-inaccessible) | sibling `<button aria-label="Delete …">` |
| `lib/storage.test.ts` | round-trip test only checked `habits.length` | added a test asserting `fasts/habitTimes/habitNotes/energy/cue/timeOfDay/selfHostUrl/fastTargetHours` all survive export→import (backup data-loss guard) |
| `docs/data-engineering/schema.sql` | drifted from `initdb.sql` | added `energy` (metrics), `cue` (habits), `note` (habit_log) |
| `README.md` | stale "33 tests" | "200+ tests" |

## Data / backups — verdict: **sound**
- `migrate()` spread-merge **preserves all new optional fields**; every field guarded with `?? []`/`?? {}` and seeded by `emptyJournal()`. No `migrate` data loss.
- `exportJSON`/`importJSON` round-trips everything (now test-covered); the Settings export inlines IndexedDB photos → self-contained backups.
- All sync paths (gist/supabase/folder/serverSync/gdrive/bujocloud) push the **whole** object — no field dropped.
- Backup nudge exists (`recommend.ts`, 7-day stale).

## Deferred (real, fix next)

| Severity | Item | Fix |
|---|---|---|
| **CRITICAL** | `App.tsx` bujocloud (passphrase) **push is unguarded LWW** — two devices sharing a passphrase can overwrite a newer remote with no prompt (the Supabase path has `resolveIncoming` + `lastSync`; this doesn't) | gate `pushCloud` on a pull+`resolveIncoming`, add a `lastSync` echo-guard. Riskier sync change — do deliberately with tests. |
| MEDIUM | `App.tsx` folder save unconditional overwrite (multi-device folder sync can lose the newer copy) | `loadFromFolder()` + `resolveIncoming` before save, or a "folder is newer" prompt |
| LOW | `save()`/encrypt failures are silent (`storage.ts`, `store.tsx`) | dispatch a `bujo:save-error` event the shell toasts |
| LOW | a11y: `FriendsCard` github-input + refresh button, `ProgramTracker` actual-input + 28px week pills, `TagManager` save title, `Cycle` active-flag contrast | add aria-labels; bump pills to `h-8`; fix contrast |
| LOW | dead prop `BottomNav.onQuickAdd` (declared/passed, unused) | remove |
| LOW | `migrate()` ignores incoming `version` (fine while additive) | branch on `data.version` before overwriting if a real transform is ever needed |
| LOW (docs) | `docs/diagrams/uml.mdx` data-model class diagram omits many fields; `FEATURE_GUIDE.md` bottom-nav line stale ("Gym" vs Today/Trackers/Fitness/Pickleball/Pull-ups), HomeWorkout undocumented | expand diagram; correct the nav line; add a HomeWorkout section |

## Verdict
No data-loss bug in migrate/export/sync-completeness (the one genuine data-loss vector — unguarded passphrase-cloud push — is documented above for a careful follow-up). 6 real code bugs fixed + tests + schema/docs corrected. Suite green (209), tsc/eslint/build clean.
