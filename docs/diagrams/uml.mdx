---
title: bujo — system UML diagrams
description: Architecture, components, data model, and sync flows as Mermaid diagrams (render on GitHub / any MDX viewer).
---

# System UML

All diagrams are [Mermaid](https://mermaid.js.org/) — they render on GitHub and in MDX. Keep them in sync with the code (see `docs/RULES.md`).

## 1. Deployment / system context

```mermaid
flowchart TB
  user([User browser / PWA])

  subgraph Vercel["Vercel (web prod)"]
    spa["bujo SPA (Vite/React)\nbujo-journal.vercel.app"]
    fn["/api/feedback (serverless)"]
  end

  subgraph SelfHost["Self-host (docker compose)"]
    web["web — nginx + SPA :8080"]
    api["api — PostgREST :3000"]
    db[("db — Postgres :5432")]
    adminer["adminer :8081"]
  end

  supa[("Supabase\n(optional account sync)")]
  gist[("GitHub Gist\n(optional backup)")]
  folder[("Local cloud folder\n(File System Access)")]

  user --> spa
  user --> web
  spa -. account .-> supa
  spa -. gist token .-> gist
  spa -. folder .-> folder
  web --> api --> db
  adminer --> db
  spa -. self-host URL .-> api

  classDef store fill:#313244,stroke:#cba6f7,color:#cdd6f4;
  class db,supa,gist,folder store;
```

bujo is **local-first**: the source of truth is the in-browser `JournalData`. Every backend (Vercel, Supabase, self-host PostgREST, gist, folder) is an *optional* sync target. With E2E, those targets hold ciphertext.

## 2. Frontend component structure

```mermaid
flowchart TD
  App[App.tsx\nNAV + view routing] --> Shell[AppShell]
  Shell --> Sidebar
  Shell --> TopBar
  Shell --> BottomNav
  Shell --> Main[(active view)]
  Shell --> MilestoneToast
  Shell --> ServerSync

  Main --> Today
  Main --> Trackers
  Main --> FitnessHub --> Fitness & Gym
  Main --> Stats
  Main --> Focus --> PomodoroCard
  Main --> NoFap
  Main --> Others[Monthly · Plan · Collections · Goals · Insights · Challenges · Pull-ups · Pickleball · HomeWorkout · Settings · Help]

  subgraph State
    store["store.tsx (useJournal)\npatch + undo/redo + persist"]
  end
  subgraph Lib["lib/*"]
    s1[stats] & s2[fasting] & s3[checkin] & s4[achievements]
    s5[milestones] & s6[timeofday] & s7[capture] & s8[serverSync]
    s9[storage/migrate] & s10[supabase/github/gdrive/fscloud]
  end

  Today & Trackers & Stats & Focus & NoFap --> store
  store --> s9
  store -. sync .-> s8 & s10
  Stats --> s3 & s4
  Today --> s2
```

## 3. Data model (class diagram)

```mermaid
classDiagram
  class JournalData {
    +int version
    +string updatedAt
    +Entry[] entries
    +Habit[] habits
    +Record~string,string[]~ habitLog
    +Record~string,Record~ habitValues
    +Record~string,Record~ habitTimes
    +Record~string,Record~ habitNotes
    +Record~string,string[]~ habitSkips
    +DailyMetric[] metrics
    +Workout[] workouts
    +Fast[] fasts
    +Routine[] routines
    +BodyMetric[] bodyMetrics
    +CyclePoint[] cycle
    +Gratitude[] gratitude
    +Memory[] memories
    +Birthday[] birthdays
    +MonthlyMeta[] monthly
    +Collection[] collections
    +Recurrence[] recurrences
    +Record~string,string[]~ stickers
    +Streak nofap
    +Challenge[] challenges
    +DevSession[] devSessions
    +PickleballSession[] pickleball
    +PickleballEvent[] pickleballEvents
    +ProgressPhoto[] progressPhotos
    +Friend[] friends
    +Book[] books
    +ReadLink[] readLinks
    +CustomGoal[] customGoals
    +MindsetFocus[] mindsetFocus
    +Settings settings
  }
  class Entry {
    +string id
    +string date
    +BulletType type
    +string text
    +EntryStatus status
    +bool important
    +bool memory
    +string[] tags
    +string collection
    +string recurringId
  }
  class Habit {
    +string id
    +string name
    +HabitCategory category
    +HabitType type
    +string color
    +string startedOn
    +int target
    +string unit
    +int[] activeDays
    +int weeklyGoal
    +bool avoid
    +string timeOfDay
    +string cue
    +string emoji
  }
  class DailyMetric {
    +string date
    +int mood
    +int stress
    +int sleep
    +int energy
    +string fastBreak
    +Weather weather
    +int calories
    +int protein
    +int carbs
    +int fat
  }
  class Workout {
    +string id
    +string date
    +string activity
    +Split split
    +int durationMin
    +int distanceKm
    +string[] sets
    +WorkoutSet[] setRows
    +int calories
    +int rpe
    +string notes
  }
  class WorkoutSet {
    +string exercise
    +int weight
    +int reps
    +int rpe
    +string kind
  }
  class Routine {
    +string id
    +string name
    +Split split
    +string[] exercises
  }
  class Fast {
    +string id
    +string start
    +string end
  }
  class BodyMetric {
    +string date
    +int weight
    +int bodyFat
    +Record measurements
  }
  class Streak {
    +string startedOn
    +int best
    +Relapse[] relapses
    +int urgesResisted
    +UrgeWin[] urgeLog
    +TriggerPlan[] plans
  }
  class Relapse {
    +string id
    +string date
    +string trigger
    +string note
  }
  class UrgeWin {
    +string id
    +string date
    +string at
    +string trigger
  }
  class TriggerPlan {
    +string id
    +string addiction
    +string trigger
    +string coping
  }
  class Book {
    +string id
    +string title
    +string author
    +BookStatus status
    +int totalPages
    +int currentPage
    +int rating
    +Learning[] learnings
  }
  class ReadLink {
    +string id
    +string url
    +string title
    +bool done
  }
  class CustomGoal {
    +string id
    +string label
    +int target
    +int value
    +string unit
  }
  class MindsetFocus {
    +string id
    +string principleId
    +string note
  }
  class Challenge {
    +string id
    +string name
    +int durationDays
    +string startDate
    +string[] rules
    +bool strict
  }
  class PickleballSession {
    +string id
    +string date
    +string format
    +int gamesWon
    +int gamesLost
    +int pointsFor
    +int pointsAgainst
  }
  class PickleballEvent {
    +string id
    +string date
    +string name
    +string kind
    +PickleballFormat format
    +string placement
  }
  class DevSession {
    +string id
    +string date
    +int durationMin
    +int focus
    +int stress
    +string[] tags
  }
  class ProgressPhoto {
    +string id
    +string date
    +string photo
    +int weight
  }
  class Friend {
    +string id
    +string name
    +string birthday
    +string github
    +string bio
  }
  class Settings {
    +Gender gender
    +WeightUnit weightUnit
    +DistanceUnit distanceUnit
    +TempUnit tempUnit
    +string storageMode
    +string selfHostUrl
    +int fastTargetHours
    +bool nofapEnabled
    +bool cycleTrackerEnabled
    +int readingGoalBooks
    +int pickleballGoalGames
    +int fitnessGoalMin
  }

  JournalData "1" *-- "many" Entry
  JournalData "1" *-- "many" Habit
  JournalData "1" *-- "many" DailyMetric
  JournalData "1" *-- "many" Workout
  JournalData "1" *-- "many" Fast
  JournalData "1" *-- "many" Routine
  JournalData "1" *-- "many" BodyMetric
  JournalData "1" *-- "many" Book
  JournalData "1" *-- "many" ReadLink
  JournalData "1" *-- "many" CustomGoal
  JournalData "1" *-- "many" MindsetFocus
  JournalData "1" *-- "many" Challenge
  JournalData "1" *-- "many" PickleballSession
  JournalData "1" *-- "many" PickleballEvent
  JournalData "1" *-- "many" DevSession
  JournalData "1" *-- "many" ProgressPhoto
  JournalData "1" *-- "many" Friend
  JournalData "1" *-- "1" Streak
  JournalData "1" *-- "1" Settings
  Workout "1" *-- "many" WorkoutSet
  Streak "1" *-- "many" Relapse
  Streak "1" *-- "many" UrgeWin
  Streak "1" *-- "many" TriggerPlan
```

## 4. Sync sequence (save → persist → flush on close)

```mermaid
sequenceDiagram
  participant U as User
  participant V as View
  participant S as store (useJournal)
  participant LS as localStorage
  participant SS as ServerSync
  participant API as PostgREST /journals

  U->>V: tap (e.g. check a habit)
  V->>S: patch(data)
  S->>LS: write JournalData (immediate)
  S-->>SS: data changed
  SS->>SS: debounce 2.5s
  SS->>API: POST /journals (upsert blob)
  API-->>SS: 201
  Note over U,SS: on tab close
  U->>SS: pagehide / visibilitychange=hidden
  SS->>API: POST /journals (keepalive flush)
```

## 5. Habit check-in state (one habit / day)

```mermaid
stateDiagram-v2
  [*] --> Unlogged
  Unlogged --> Done: toggle (records habitTimes)
  Done --> Unlogged: toggle off
  Unlogged --> Skipped: planned skip (streak-safe)
  Skipped --> Unlogged
  note right of Done
    avoid habit → Done means SLIP (red);
    streak = clean days (cleanStreak)
  end note
```

## Regenerating
These are hand-maintained from the code. When the data model, component tree, or sync paths change, update the relevant diagram in the **same PR** — `docs/RULES.md` makes this a checklist item, and `.github/workflows/docs-guard.yml` flags code-only PRs.
