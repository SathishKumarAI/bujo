/**
 * Modernist band primitives. One entrypoint, so a page imports the layer rather
 * than three files — and so a new primitive lands in one place. See README.md
 * for the change → file table.
 */
export { Band, BandRow, BandCell } from './Band'
export { Eyebrow } from './Eyebrow'
export { Statement } from './Statement'
