# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local reference PDFs — never commit (may contain personal info / copyrighted programs)
docs/pdf/

# graphify — generated knowledge-graph state (machine-local, rebuildable)
.graphify/
# Personal per-machine Claude Code settings (not the shared project config)
.claude/settings.local.json
.vercel
.env*

# BUJO-193 self-host API TLS certs (do not commit)
docker/certs/
.env
